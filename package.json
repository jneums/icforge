{
  "name": "atlascloud",
  "private": true,
  "workspaces": [
    "cli",
    "dashboard",
    "shared"
  ],
  "scripts": {
    "cli": "npm -w cli run",
    "dashboard": "npm -w dashboard run",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
