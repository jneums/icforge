import chalk from "chalk";

export async function deployCommand(options: Record<string, unknown> = {}) {
  console.log(chalk.cyan("icforge deploy"), "— not yet implemented");
  // TODO: implement deploy command
}
